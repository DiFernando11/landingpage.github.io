import AlertCount from './AlertCount';
import Banner from './Banner';
import NavRoutesPage from './NavRoutesPage';

const Header = () => {
  return (
    <header>
      <AlertCount />
      <Banner />
      <NavRoutesPage />
    </header>
  );
};

export default Header;
